[package]
name = "miso-midi-native-score-bench"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Standalone native score parser and diagnostic-floor benchmark harness"

[dependencies]
miso-midi-core = { path = "../../crates/miso-midi-core" }

[lints]
workspace = true
